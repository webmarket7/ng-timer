include ../../../../../views/mixins/button.pug

.timer
    .timer_face
        .timer_controls
            +button({
                attr: {'#toggleButton': ''},
                mode: {'[ngClass]': "(clickActions$ | async) || 'stopped'"}
            })
        .timer_digits {{ (timerService.timer$ | async)?.value | time }}
