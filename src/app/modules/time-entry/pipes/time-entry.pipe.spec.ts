import { TimeEntryPipe } from './time-entry.pipe';

describe('TimeEntryPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeEntryPipe();
    expect(pipe).toBeTruthy();
  });
});
